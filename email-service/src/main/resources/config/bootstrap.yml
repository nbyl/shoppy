spring:
    application:
        name: email
    cloud:
        config:
            discovery:
                enabled: true
            failFast: true
    mail:
        host: localhost
        port: 250
server:
    port: 8083
