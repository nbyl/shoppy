spring:
    application:
        name: email
    cloud:
        config:
            discovery:
                enabled: true
            failFast: true
    mail:
        host: localhost
server:
    port: 8083
